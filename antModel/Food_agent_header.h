/**
 * \file  Food_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def FOODID
 * \brief Direct access to foodID of Food agent memory variable. */
#define FOODID (current_xmachine_Food->foodID)
/** \def SIZE
 * \brief Direct access to size of Food agent memory variable. */
#define SIZE (current_xmachine_Food->size)
/** \def FOODX
 * \brief Direct access to foodX of Food agent memory variable. */
#define FOODX (current_xmachine_Food->foodX)
/** \def FOODY
 * \brief Direct access to foodY of Food agent memory variable. */
#define FOODY (current_xmachine_Food->foodY)
/** \def RADIUS
 * \brief Direct access to radius of Food agent memory variable. */
#define RADIUS (current_xmachine_Food->radius)
